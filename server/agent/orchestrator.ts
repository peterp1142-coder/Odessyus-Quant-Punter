import type { ReActStep } from './react-engine.js';
import { runOddsScout } from './subagents/odds-scout.js';
import { runFormScout } from './subagents/form-scout.js';
import { runInjuryIntel } from './subagents/injury-intel.js';
import { runSentimentAgent } from './subagents/sentiment-agent.js';
import { runLineupScout } from './subagents/lineup-scout.js';
import { runRefereeScout } from './subagents/referee-scout.js';
import { runTacticalScout } from './subagents/tactical-scout.js';
import { runDataQualityScout } from './subagents/data-quality-scout.js';
import { runMarketMicrostructureScout } from './subagents/market-microstructure.js';
import { runModelRiskScout } from './subagents/model-risk.js';
import { runQuantSynthesis } from './subagents/quant-synthesis.js';
import { mistralPool } from './mistral-pool.js';
import type { SubAgentResult } from './subagents/base.js';
import { logPrediction } from './airtable-logger.js';
import { broadFixtureDiscovery } from './broad-fixture-sources.js';
import { fixtureScheduler } from './fixture-task-scheduler.js';
import { researchCacheStats } from './research-cache.js';

export interface OrchestratorResult { finalAnswer:string; steps:ReActStep[]; success:boolean; error?:string; metadata:any; }
type AgentName='OddsScout'|'FormScout'|'InjuryIntel'|'SentimentAgent'|'LineupScout'|'RefereeScout'|'TacticalScout'|'DataQualityScout'|'MarketMicrostructureScout'|'ModelRiskScout';
interface AgentTask{name:AgentName;focus:string;required:boolean;tier:number;}
interface AgentPlan{reasoning:string;agents:AgentTask[];}
interface DiscoveredFixture{fixture:string;kickoff?:string;status?:string;competition?:string;}
interface SummaryRow{league:string;fixture:string;date:string;market:string;selection:string;probability:number|null;odds:number|null;ev:number|null;confidence:number;stars:number;status:string;}

const MARKET_TIMEZONE=process.env.MARKET_TIMEZONE||'Africa/Lagos';
const MAX_DISCOVERY_FIXTURES=Math.max(1,Number(process.env.MAX_DISCOVERY_FIXTURES||20));
const MAX_FIXTURE_PIPELINES=Math.max(1,Number(process.env.MAX_FIXTURE_PIPELINES||3));

function marketToday(){return new Intl.DateTimeFormat('en-CA',{timeZone:MARKET_TIMEZONE,year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());}
function fixtureMatchDate(k?:string,fallback=''){if(!k)return fallback;const d=new Date(k);if(Number.isNaN(d.getTime()))return fallback;return new Intl.DateTimeFormat('en-CA',{timeZone:MARKET_TIMEZONE,year:'numeric',month:'2-digit',day:'2-digit'}).format(d);}
function normalizeFixtureName(v:string){return v.toLowerCase().replace(/\s+/g,' ').replace(/\s*(?:vs\.?|v\.?)\s*/g,' vs ').trim();}
function normalizeLeague(value?:string){const s=String(value||'').replace(/\s+/g,' ').trim();if(!s)return'UNVERIFIED LEAGUE';const x=s.toLowerCase();if(x.includes('premier league')&&!x.includes('scotland'))return'PREMIER LEAGUE';if(x.includes('championship')&&x.includes('england'))return'CHAMPIONSHIP';if(x.includes('serie a'))return'SERIE A';if(x.includes('serie b'))return'SERIE B';if(x.includes('la liga')||x.includes('primera división')||x.includes('primera division'))return'LA LIGA';if(x.includes('bundesliga'))return'BUNDESLIGA';if(x.includes('ligue 1'))return'LIGUE 1';if(x.includes('eredivisie'))return'EREDIVISIE';if(x.includes('primeira liga'))return'PRIMEIRA LIGA';if(x.includes('champions league'))return'UEFA CHAMPIONS LEAGUE';if(x.includes('europa league'))return'UEFA EUROPA LEAGUE';return s.toUpperCase();}
function looksLikeRealFixture(v:string){const s=v.replace(/\s+/g,' ').trim();if(!/^.{2,70}\s+(?:vs\.?|v\.?)\s+.{2,70}$/i.test(s))return false;if(/(?:sky sports|flashscore|follow |live |schedule|fixture|fifa world cup|today's matches|matches taking place|brings you|kick-off|match time|odds by|premier league schedule|espn)/i.test(s))return false;if(/\b(?:saturday|sunday|monday|tuesday|wednesday|thursday|friday),?\s+\d{1,2}\s+/i.test(s))return false;if(/\bweek\s*\d+\b.*\bjan\.?\b/i.test(s))return false;if(/^week\s*\d+/i.test(s))return false;if(/\b(?:nfl|nba|mlb|nhl)\b/i.test(s))return false;return true;}
function isCompletedStatus(status?:string){const s=String(status||'').toLowerCase();return/finished|final|ended|ft|aet|cancelled|canceled|postponed|abandoned|walkover|live|in.?play|half.?time/.test(s);}
function balancedJsonCandidates(raw:string){const text=raw.replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/i,'').trim();const out:string[]=[];for(const open of ['{','[']){let start=-1,depth=0,str=false,esc=false;for(let i=0;i<text.length;i++){const c=text[i];if(start<0){if(c===open){start=i;depth=1;}continue;}if(str){if(esc)esc=false;else if(c==='\\')esc=true;else if(c==='"')str=false;continue;}if(c==='"'){str=true;continue;}if(c===open)depth++;else if((open==='{'&&c==='}')||(open==='['&&c===']'))depth--;if(depth===0){out.push(text.slice(start,i+1));start=-1;}}}return out.sort((a,b)=>b.length-a.length);}
function parseFixturePayload(raw:string):DiscoveredFixture[]{for(const candidate of balancedJsonCandidates(raw)){try{const v:any=JSON.parse(candidate);const list=Array.isArray(v)?v:v?.fixtures;if(!Array.isArray(list))continue;const clean=list.filter((x:any)=>x&&typeof x.fixture==='string'&&looksLikeRealFixture(x.fixture)).map((x:any)=>({fixture:x.fixture.trim(),kickoff:typeof x.kickoff==='string'?x.kickoff:undefined,status:typeof x.status==='string'?x.status:'scheduled',competition:typeof x.competition==='string'?x.competition:undefined}));if(clean.length)return clean;}catch{}}return[];}
function extractStructuredFallback(raw:string):DiscoveredFixture[]{const out:DiscoveredFixture[]=[];for(const c of balancedJsonCandidates(raw)){try{const root:any=JSON.parse(c);const walk=(n:any)=>{if(!n||typeof n!=='object')return;if(Array.isArray(n)){for(const x of n)walk(x);return;}const home=n.homeTeam?.name||n.home_team?.name||n.home?.name||n.homeTeam||n.home_team||n.home;const away=n.awayTeam?.name||n.away_team?.name||n.away?.name||n.awayTeam||n.away_team||n.away;const kickoff=n.kickoff||n.startTime||n.start_time||n.date||n.utcDate||n.utc_date;const status=n.status?.type?.short||n.status?.short||n.status||n.fixture?.status?.short;const fixture=typeof home==='string'&&typeof away==='string'?`${home} vs ${away}`:'';if(fixture&&looksLikeRealFixture(fixture)&&typeof kickoff==='string')out.push({fixture,kickoff,status:typeof status==='string'?status:'scheduled',competition:n.competition?.name||n.league?.name});for(const child of Object.values(n))walk(child);};walk(root);}catch{}}return out;}
function dedupeFixtures(values:DiscoveredFixture[],requestedDate:string){const seen=new Set<string>(),out:DiscoveredFixture[]=[];for(const v of values){if(!v.fixture||!looksLikeRealFixture(v.fixture)||isCompletedStatus(v.status))continue;if(v.kickoff){const d=new Date(v.kickoff);if(Number.isNaN(d.getTime()))continue;if(d.getTime()<Date.now()-120000)continue;if(fixtureMatchDate(v.kickoff,requestedDate)!==requestedDate)continue;}const key=normalizeFixtureName(v.fixture);if(seen.has(key))continue;seen.add(key);out.push(v);if(out.length>=MAX_DISCOVERY_FIXTURES)break;}return out;}

async function discoverFixtures(userQuery:string,sport:string,date:string,onStep:(s:ReActStep)=>void){onStep({type:'status',content:`🔍 Discovering real ${sport} fixtures for ${date} in ${MARKET_TIMEZONE}…`});const{fetchMatchesToday,allSportsFixtures}=await import('./tools.js');const[broad,web,api]=await Promise.allSettled([broadFixtureDiscovery(date,sport),fetchMatchesToday(sport,date),sport.toLowerCase()==='football'?allSportsFixtures(date,date):Promise.resolve({success:false,data:'',error:'unsupported sport'})]);const chunks:string[]=[];for(const r of[broad,web,api])if(r.status==='fulfilled'&&r.value?.success&&r.value?.data)chunks.push(String(r.value.data));const combined=chunks.join('\n\n');if(!combined){onStep({type:'error',content:`No fixture source returned data for ${date}.`});return{fixtures:[],rawSchedule:''};}let modelFixtures:DiscoveredFixture[]=[];try{const r=await mistralPool.call(c=>c.chat.complete({model:'mistral-small-latest',messages:[{role:'system',content:'Extract ONLY explicit real sporting fixtures from the supplied schedule text. Return JSON only: {"fixtures":[{"fixture":"Home vs Away","kickoff":"ISO-8601","status":"scheduled","competition":"..."}]}. Never return article titles, source descriptions, league names, headings, or sentences containing a fixture as part of a headline. The fixture field must contain ONLY two team names joined by vs. Kickoff must come from the supplied text; never invent dates or times.'},{role:'user',content:`Requested market date: ${date}\nTimezone: ${MARKET_TIMEZONE}\nUser request: ${userQuery}\nSUPPLIED SCHEDULE DATA:\n${combined.slice(0,30000)}`}] as any,temperature:0,maxTokens:Math.min(5000,200+MAX_DISCOVERY_FIXTURES*140)}));modelFixtures=parseFixturePayload(String(r.choices?.[0]?.message?.content||''));}catch(e){onStep({type:'error',content:`Structured fixture extraction failed: ${e instanceof Error?e.message:String(e)}`});}const structured=extractStructuredFallback(combined);const fixtures=dedupeFixtures([...modelFixtures,...structured],date);if(fixtures.length)onStep({type:'thought',content:`📋 ${fixtures.length} real fixtures admitted: ${fixtures.map(f=>`${f.fixture} [${fixtureMatchDate(f.kickoff,date)}]`).join(' | ')}`});else onStep({type:'error',content:`No clean fixtures passed the identity/date gate for ${date}.`});return{fixtures,rawSchedule:combined};}

async function parseQuery(userQuery:string){try{const r=await mistralPool.call(c=>c.chat.complete({model:'mistral-small-latest',messages:[{role:'system',content:'Return JSON only.'},{role:'user',content:`Extract sport, requested market and whether the user named one fixture or wants multiple/open fixtures from: ${userQuery}. Return {"fixture":"Team A vs Team B or multiple/open","sport":"football","market":"Match Result","matchDate":"today or YYYY-MM-DD"}.`} ] as any,temperature:0,maxTokens:200}));for(const c of balancedJsonCandidates(String(r.choices?.[0]?.message?.content||''))){try{const p:any=JSON.parse(c);return{fixture:p.fixture||'multiple/open',sport:p.sport||'football',market:p.market||'Match Result',matchDate:p.matchDate==='today'||!p.matchDate?marketToday():String(p.matchDate)};}catch{}}}catch{}return{fixture:'multiple/open',sport:'football',market:'Match Result',matchDate:marketToday()};}
async function buildPlan(fixture:string):Promise<AgentPlan>{return{reasoning:'Fixture-scoped dependency DAG with independent evidence and portfolio review.',agents:[{name:'OddsScout',focus:`Verify exact-market odds for ${fixture}, plus other liquid markets explicitly available for the fixture.`,required:false,tier:1},{name:'FormScout',focus:`Verify current-season form, xG, venue splits and H2H for ${fixture}.`,required:true,tier:1},{name:'InjuryIntel',focus:`Verify injuries, suspensions and availability for ${fixture}.`,required:false,tier:2},{name:'SentimentAgent',focus:`Verify team news, motivation and external context for ${fixture}.`,required:false,tier:2},{name:'RefereeScout',focus:`Verify referee evidence for ${fixture}.`,required:false,tier:2},{name:'MarketMicrostructureScout',focus:`Verify market dispersion and liquidity for ${fixture}.`,required:false,tier:2},{name:'LineupScout',focus:`Verify confirmed/predicted lineups for ${fixture}.`,required:false,tier:3},{name:'TacticalScout',focus:`Assess tactical matchup for ${fixture}.`,required:false,tier:3},{name:'ModelRiskScout',focus:`Stress-test all candidate picks for ${fixture}.`,required:false,tier:4},{name:'DataQualityScout',focus:`Audit evidence freshness and contradictions for ${fixture}.`,required:false,tier:4}]};}
function canonicalFixtureContext(fixture:string,sport:string,matchDate:string,competition?:string){return`=== CANONICAL FIXTURE REGISTRY ENTRY ===\nFixture: ${fixture}\nSport: ${sport}\nMatch date: ${matchDate}\nCompetition: ${competition||'verified registry'}\nRULE: Analyze ONLY this fixture/date. Never substitute another match.\n=== END CANONICAL FIXTURE REGISTRY ENTRY ===`;}
async function dispatchOne(task:AgentTask,fixture:string,sport:string,matchDate:string,sessionId:string,prior:string,onStep:(s:ReActStep)=>void):Promise<SubAgentResult>{const taskText=`${canonicalFixtureContext(fixture,sport,matchDate)}\n\n${task.focus}\n\n${prior}`;switch(task.name){case'OddsScout':return runOddsScout(fixture,sport,sessionId,onStep,taskText);case'FormScout':return runFormScout(fixture,sport,sessionId,onStep,taskText);case'InjuryIntel':return runInjuryIntel(fixture,sport,sessionId,onStep,taskText);case'SentimentAgent':return runSentimentAgent(fixture,sport,matchDate,sessionId,onStep,taskText);case'LineupScout':return runLineupScout(fixture,sport,sessionId,onStep,taskText);case'RefereeScout':return runRefereeScout(fixture,sport,sessionId,onStep,taskText);case'TacticalScout':return runTacticalScout(fixture,sport,sessionId,onStep,taskText);case'DataQualityScout':return runDataQualityScout(fixture,sport,sessionId,onStep,taskText);case'MarketMicrostructureScout':return runMarketMicrostructureScout(fixture,sport,sessionId,onStep,taskText);case'ModelRiskScout':return runModelRiskScout(fixture,sport,sessionId,onStep,taskText);default:throw new Error(`Unsupported agent ${task.name}`);}}
async function dispatchAgent(task:AgentTask,fixture:string,sport:string,matchDate:string,sessionId:string,prior:string,onStep:(s:ReActStep)=>void){return fixtureScheduler.enqueue({agentName:task.name,fixture,tier:task.tier,run:()=>dispatchOne(task,fixture,sport,matchDate,sessionId,prior,s=>onStep({...s,content:`[${fixture}] ${s.content}`}))},position=>onStep({type:'status',content:`⏳ ${task.name}: ${fixture} queued behind ${position} job(s).`}));}
function priorContext(results:Record<string,SubAgentResult>){return Object.entries(results).map(([name,r])=>`[${name}] ${(r.rawOutput||JSON.stringify(r.data)).slice(0,2200)}`).join('\n\n');}
async function runFixturePipeline(fx:DiscoveredFixture,index:number,plan:AgentPlan,schedule:string,sessionId:string,sport:string,requestedDate:string,onStep:(s:ReActStep)=>void){const matchDate=fixtureMatchDate(fx.kickoff,requestedDate),results:Record<string,SubAgentResult>={},canonical=canonicalFixtureContext(fx.fixture,sport,matchDate,fx.competition);for(const tier of[1,2,3,4]){const tasks=plan.agents.filter(a=>a.tier===tier);const settled=await Promise.allSettled(tasks.map(t=>dispatchAgent(t,fx.fixture,sport,matchDate,`${sessionId}-${index}-${t.name}`,`${canonical}\n=== DISCOVERY ===\n${schedule.slice(0,5000)}\n${priorContext(results)}`,onStep)));settled.forEach((r,i)=>{if(r.status==='fulfilled')results[tasks[i].name]=r.value;else results[tasks[i].name]={agentName:tasks[i].name,success:false,data:{},steps:[],rawOutput:'',error:String(r.reason)};});}return results;}
function aggregateOne(name:AgentName,fixture:string,results:Record<string,SubAgentResult>):SubAgentResult{return results[name]||{agentName:name,success:false,data:{},steps:[],rawOutput:'',error:`No ${name} result for ${fixture}`};}
function advancedText(results:Record<string,SubAgentResult>){return['RefereeScout','TacticalScout','MarketMicrostructureScout','ModelRiskScout','DataQualityScout'].map(n=>results[n]?.rawOutput||'').filter(Boolean).join('\n\n');}

async function extractMarketPicks(fixture:string,answer:string,q:any):Promise<Array<{market:string;selection:string;probability:number|null;odds:number|null;ev:number|null;confidence:number;stars:number;status:string}>>{const fallback={market:q?.market||'Match Result',selection:'MODEL LEAD',probability:Number.isFinite(q?.trueProb)?q.trueProb*100:null,odds:q?.recommendedOdds>1?q.recommendedOdds:null,ev:Number.isFinite(q?.expectedValue)?q.expectedValue*100:null,confidence:q?.confidence||0,stars:q?.starRating||0,status:q?.isValueBet?'BET':'SKIP'};try{const r=await mistralPool.call(c=>c.chat.complete({model:'mistral-small-latest',messages:[{role:'system',content:'Extract ONLY explicit betting/prediction picks already present in the supplied fixture analysis. Do not invent markets, prices, probabilities or value. A fixture may have multiple picks across 1X2, double chance, DNB, Asian handicap, BTTS, Over/Under totals, cards, corners, player props or other markets. Return JSON only as {"picks":[{"market":"1X2|BTTS|TOTALS|DNB|ASIAN_HANDICAP|CARDS|CORNERS|PLAYER_PROP|OTHER","selection":"exact selection text","probability_pct":number|null,"odds":number|null,"ev_pct":number|null,"status":"BET|CONSIDER|SKIP|UNVALIDATED"}]}. Copy numbers only when explicitly stated. Do not convert implied probability into model probability. Keep at most 8 strongest explicit picks. If the analysis gives no explicit pick, return an empty list.'},{role:'user',content:`FIXTURE: ${fixture}\nDETERMINISTIC PRIMARY MODEL: ${JSON.stringify({probability:q?.trueProb,odds:q?.recommendedOdds,ev:q?.expectedValue,confidence:q?.confidence,stars:q?.starRating,isValueBet:q?.isValueBet})}\nANALYSIS:\n${answer.slice(0,14000)}`}] as any,temperature:0,maxTokens:1800}));const raw=String(r.choices?.[0]?.message?.content||'');for(const c of balancedJsonCandidates(raw)){try{const parsed:any=JSON.parse(c);if(!Array.isArray(parsed?.picks))continue;const picks=parsed.picks.filter((p:any)=>p&&typeof p.market==='string'&&typeof p.selection==='string').map((p:any)=>({market:p.market,selection:p.selection,probability:Number.isFinite(Number(p.probability_pct))?Number(p.probability_pct):null,odds:Number.isFinite(Number(p.odds))?Number(p.odds):null,ev:Number.isFinite(Number(p.ev_pct))?Number(p.ev_pct):null,confidence:q?.confidence||0,stars:q?.starRating||0,status:['BET','CONSIDER','SKIP','UNVALIDATED'].includes(String(p.status))?String(p.status):'UNVALIDATED'}));if(picks.length)return picks;}catch{}}}catch{}return[fallback];}
function formatSummaryTable(groups:Record<string,SummaryRow[]>) {const chunks:string[]=['# FIXTURE & PICKS SUMMARY','Grouped by verified league. Each fixture may contribute multiple explicit market opportunities; this is not restricted to 1X2.',''];for(const league of Object.keys(groups).sort()){chunks.push(`## ${league}`,'','| Fixture | Date | Market | Selection | Model % | Odds | EV | Confidence | Stars | Status |','|---|---|---|---|---:|---:|---:|---:|---:|---|');for(const r of groups[league].sort((a,b)=>a.fixture.localeCompare(b.fixture)||a.market.localeCompare(b.market))){const p=r.probability==null?'—':`${r.probability.toFixed(1)}%`;const odds=r.odds!=null?r.odds.toFixed(2):'—';const ev=r.ev==null?'—':`${r.ev>=0?'+':''}${r.ev.toFixed(2)}%`;chunks.push(`| ${r.fixture} | ${r.date} | ${r.market} | **${r.selection}** | ${p} | ${odds} | ${ev} | ${r.confidence}% | ${r.stars}/5 | ${r.status} |`);}chunks.push('');}return chunks.join('\n');}

export async function runOrchestrator(userQuery:string,sessionId:string,onStep:(step:ReActStep)=>void):Promise<OrchestratorResult>{const steps:ReActStep[]=[];const emit=(s:ReActStep)=>{const x={...s,timestamp:new Date().toISOString()};steps.push(x);onStep(x);};const parsed=await parseQuery(userQuery);let fixtures:DiscoveredFixture[]=[];let schedule='';if(parsed.fixture==='multiple/open'||parsed.fixture===userQuery){const d=await discoverFixtures(userQuery,parsed.sport,parsed.matchDate,emit);fixtures=d.fixtures;schedule=d.rawSchedule;}else fixtures=[{fixture:parsed.fixture,status:'scheduled'}];if(!fixtures.length)return{finalAnswer:`NO QUALIFIED FIXTURES: No clean future fixtures were verified for ${parsed.matchDate}.`,steps,success:true,metadata:{fixture:'',sport:parsed.sport,market:parsed.market,matchDate:parsed.matchDate,fixtures:[]}};const plan=await buildPlan(fixtures.map(f=>f.fixture).join(' | '));const registry:Record<string,Record<string,SubAgentResult>>={};let next=0;const workers=Array.from({length:Math.min(MAX_FIXTURE_PIPELINES,fixtures.length)},async()=>{while(true){const i=next++;if(i>=fixtures.length)return;registry[fixtures[i].fixture]=await runFixturePipeline(fixtures[i],i,plan,schedule,sessionId,parsed.sport,parsed.matchDate,emit);}});await Promise.all(workers);const outputs:string[]=[];const predictions:any[]=[];const summaryGroups:Record<string,SummaryRow[]>={};for(let i=0;i<fixtures.length;i++){const fx=fixtures[i],results=registry[fx.fixture]||{},date=fixtureMatchDate(fx.kickoff,parsed.matchDate),league=normalizeLeague(fx.competition);emit({type:'status',content:`🧮 Synthesizing ${fx.fixture} independently for ${date}…`});const q=await runQuantSynthesis({userQuery,fixture:fx.fixture,sport:parsed.sport,market:parsed.market,sessionId:`${sessionId}-synthesis-${i}`,oddsResult:aggregateOne('OddsScout',fx.fixture,results),formResult:aggregateOne('FormScout',fx.fixture,results),injuryResult:aggregateOne('InjuryIntel',fx.fixture,results),sentimentResult:aggregateOne('SentimentAgent',fx.fixture,results),lineupResult:aggregateOne('LineupScout',fx.fixture,results),advancedText:advancedText(results),onStep:emit,isMultiFixture:false,discoveredFixtures:[fx.fixture]});const picks=await extractMarketPicks(fx.fixture,q.finalAnswer||'',q);for(const p of picks){(summaryGroups[league] ||= []).push({league,fixture:fx.fixture,date,market:p.market,selection:p.selection,probability:p.probability,odds:p.odds,ev:p.ev,confidence:p.confidence,stars:p.stars,status:p.status});}outputs.push(`## ${fx.fixture}\n**League:** ${league}\n**Match date:** ${date}\n\n${q.finalAnswer||'NO QUALIFIED ANALYSIS'}`);predictions.push({fixture:fx.fixture,league,date,success:q.success,primaryMarket:parsed.market,primaryProbability:q.trueProb*100,confidence:q.confidence,starRating:q.starRating,expectedValue:q.expectedValue,isValueBet:q.isValueBet,picks});if(q.success)logPrediction({predictionId:`${sessionId}-${i}-${Date.now()}`,fixture:fx.fixture,sport:parsed.sport,market:parsed.market,predictedProb:q.trueProb,impliedProb:q.impliedProb,expectedValue:q.expectedValue,isValueBet:q.isValueBet,starRating:q.starRating,league:fx.competition});}const summary=formatSummaryTable(summaryGroups);const finalAnswer=`${summary}\n\n# DETAILED FIXTURE ANALYSIS\n\n${outputs.join('\n\n')}`;emit({type:'synthesis',content:`🏆 Independent synthesis complete for ${outputs.length}/${fixtures.length} verified fixtures; grouped ${Object.keys(summaryGroups).length} leagues and surfaced ${Object.values(summaryGroups).reduce((n,v)=>n+v.length,0)} explicit market picks.`});return{finalAnswer,steps,success:true,metadata:{fixture:fixtures.map(f=>f.fixture).join(' | '),sport:parsed.sport,market:parsed.market,matchDate:parsed.matchDate,fixtures:fixtures.map(f=>({fixture:f.fixture,kickoff:f.kickoff,date:fixtureMatchDate(f.kickoff,parsed.matchDate),league:normalizeLeague(f.competition)})),predictions,summaryByLeague:summaryGroups,agentsRun:plan.agents.map(a=>a.name),queueSnapshot:fixtureScheduler.snapshot(),researchCache:researchCacheStats(),fixtureRegistry:Object.fromEntries(Object.entries(registry).map(([fx,r])=>[fx,Object.fromEntries(Object.entries(r).map(([n,v])=>[n,v.success]))])),poolStatus:mistralPool.status()}};
}
