import type { ReActStep } from '../react-engine.js';
import type { SubAgentResult } from './base.js';
import { buildSystemPrompt } from '../prompts.js';
import { mistralPool } from '../mistral-pool.js';
import { saveCheckpoint, loadCheckpoint, clearCheckpoint } from '../checkpoint.js';
import { runMonteCarlo } from '../monte-carlo.js';
import { score, calcKelly } from '../scorer.js';
import { extractFeatures } from '../feature-extractor.js';
export interface QuantResult{finalAnswer:string;steps:ReActStep[];success:boolean;error?:string;monteCarlo:{home:number;draw:number;away:number;stdDev:number};trueProb:number;impliedProb:number;expectedValue:number;starRating:number;dataCompletenessScore:number;isValueBet:boolean;recommendedStake:string;recommendedOdds:number;confidence:number;goalStatement:string;categoryProbabilities:{market:number;form:number;injury:number;sentiment:number};}
async function synthesiseMultiFixture(args:any):Promise<QuantResult>{const{userQuery,sport,oddsResult,formResult,injuryResult,sentimentResult,discoveredFixtures,emit,steps}=args;emit({type:'status',content:`Generating ranked value-bet shortlist across ${discoveredFixtures.length} fixtures…`});const raw=oddsResult.rawOutput||'';const form=formResult.success?(formResult.rawOutput||'').slice(0,1200):'FAILED';const inj=injuryResult.success?(injuryResult.rawOutput||'').slice(0,900):'FAILED';const sent=sentimentResult.success?(sentimentResult.rawOutput||'').slice(0,700):'FAILED';const prompt=`${buildSystemPrompt()}\nYou are a strict value-bet ranker. Use ONLY verified odds from OddsScout. Do not calculate or invent EV. If selection odds or market identity is missing, exclude the pick.\nUSER: ${userQuery}\nDATE: ${new Date().toISOString().slice(0,10)}\nFIXTURES: ${discoveredFixtures.join(' | ')}\nODDS SCOUT:\n${raw.slice(0,5000)}\nFORM:\n${form}\nINJURY:\n${inj}\nSENTIMENT:\n${sent}\nReturn FINAL_ANSWER with ranked picks. Every pick must preserve the exact fixture + market + verified odds from OddsScout; never substitute another market. If none are verified, say no verified value bets.`;try{const r=await mistralPool.call(c=>c.chat.complete({model:'mistral-large-latest',messages:[{role:'system',content:prompt},{role:'user',content:'Produce the verified shortlist.'}]as any,temperature:.05,maxTokens:4000}));const content=r.choices?.[0]?.message?.content;if(typeof content!=='string'||!content)throw new Error('Empty multi-fixture synthesis');const finalAnswer=content.includes('FINAL_ANSWER:')?content.slice(content.indexOf('FINAL_ANSWER:')+13).trim():content;return{finalAnswer,steps,success:true,monteCarlo:{home:0,draw:0,away:0,stdDev:0},trueProb:0,impliedProb:0,expectedValue:0,starRating:0,dataCompletenessScore:0,isValueBet:false,recommendedStake:'SEE VERIFIED SHORTLIST',recommendedOdds:0,confidence:0,goalStatement:`Verified shortlist — ${discoveredFixtures.length} fixtures`,categoryProbabilities:{market:0,form:0,injury:0,sentiment:0}}}catch(e){const msg=e instanceof Error?e.message:String(e);return{finalAnswer:`⚠️ No verified shortlist: ${msg}`,steps,success:false,error:msg,monteCarlo:{home:0,draw:0,away:0,stdDev:0},trueProb:0,impliedProb:0,expectedValue:0,starRating:0,dataCompletenessScore:0,isValueBet:false,recommendedStake:'SKIP',recommendedOdds:0,confidence:0,goalStatement:'',categoryProbabilities:{market:0,form:0,injury:0,sentiment:0}}}}
export async function runQuantSynthesis(opts:{userQuery:string;fixture:string;sport:string;market:string;sessionId?:string;oddsResult:SubAgentResult;formResult:SubAgentResult;injuryResult:SubAgentResult;sentimentResult:SubAgentResult;lineupResult?:SubAgentResult;onStep:(step:ReActStep)=>void;isMultiFixture?:boolean;discoveredFixtures?:string[]}):Promise<QuantResult>{const{userQuery,fixture,sport,market='Match Result',sessionId='default',oddsResult,formResult,injuryResult,sentimentResult,lineupResult,onStep,isMultiFixture=false,discoveredFixtures=[]}=opts;const steps:ReActStep[]=[];const emit=(s:ReActStep)=>{const x={...s,timestamp:new Date().toISOString()};steps.push(x);onStep(x)};const existing=await loadCheckpoint(sessionId,'QuantSynthesis');if(existing){await clearCheckpoint(sessionId,'QuantSynthesis');}if(isMultiFixture&&oddsResult.success&&oddsResult.rawOutput)return synthesiseMultiFixture({userQuery,sport,oddsResult,formResult,injuryResult,sentimentResult,discoveredFixtures,emit,steps});
emit({type:'status',content:'Extracting numeric features from scout reports…'});const lineupText=lineupResult?.success?(lineupResult.rawOutput||JSON.stringify(lineupResult.data)):'';const features=await extractFeatures({formText:formResult.success?(formResult.rawOutput||JSON.stringify(formResult.data)):'',oddsText:oddsResult.success?(oddsResult.rawOutput||JSON.stringify(oddsResult.data)):'',injuryText:[injuryResult.success?(injuryResult.rawOutput||JSON.stringify(injuryResult.data)):'',lineupText].filter(Boolean).join('\n=== LINEUP ===\n'),sentimentText:sentimentResult.success?(sentimentResult.rawOutput||JSON.stringify(sentimentResult.data)):'',fixture,market});const injuryAdjustHome=Math.min(.5,(features.injury.injuryIndexHome/10)*.4+((features.injury.absentPlayerRatingHome??0)/10)*.1);const injuryAdjustAway=Math.min(.5,(features.injury.injuryIndexAway/10)*.4+((features.injury.absentPlayerRatingAway??0)/10)*.1);const mc=runMonteCarlo({xgHome:features.form.xgHome,xgAway:features.form.xgAway,homeAdvantage:.15,dixonColesRho:-.1,injuryAdjustHome,injuryAdjustAway});const selectionOdds=features.market.selectionOdds;const selectionImplied=features.market.selectionImpliedProb;const scored=score({market,mcResult:mc,marketSignals:features.market,formSignals:features.form,injurySignals:features.injury,sentimentSignals:features.sentiment,targetOdds:selectionOdds});const trueProb=scored.finalProbability;const impliedProb=selectionImplied??(selectionOdds&&selectionOdds>1?1/selectionOdds:0);const ev=scored.expectedValue;const kelly=calcKelly(trueProb,selectionOdds??0);const gate=scored.isValueBet?'PASS':'FAIL';emit({type:'thought',content:`${scored.calibrationNote} | Gate ${gate}`});const topScores=Object.entries(mc.correctScoreDistribution).sort((a,b)=>b[1]-a[1]).slice(0,6).map(([s,p])=>`${s} (${(p*100).toFixed(1)}%)`).join(', ');const prompt=`${buildSystemPrompt()}\nQUANTITATIVE SYNTHESIS. Math is final; do not recalculate. TARGET MARKET: ${market}\nFixture: ${fixture}\nVerified selection odds: ${selectionOdds&&selectionOdds>1?selectionOdds:'UNVERIFIED'}\nSelection implied probability: ${impliedProb?`${(impliedProb*100).toFixed(1)}%`:'UNVERIFIED'}\nTrue probability: ${(trueProb*100).toFixed(1)}%\nEV: ${(ev*100).toFixed(2)}%\nGate: ${gate} (${scored.gateFailReason||'passed'})\nStars: ${scored.starRating}/5 | Stake: ${scored.recommendedStake}\nKelly half: ${kelly.halfKelly}%\nMC: Home ${(mc.homeWin*100).toFixed(1)}%, Draw ${(mc.draw*100).toFixed(1)}%, Away ${(mc.awayWin*100).toFixed(1)}%, BTTS ${(mc.btts*100).toFixed(1)}%, O2.5 ${(mc.over25*100).toFixed(1)}%, U2.5 ${(mc.under25*100).toFixed(1)}%\nTop scores: ${topScores}\nDATA:\n${oddsResult.rawOutput?.slice(0,1600)||'Odds unavailable'}\n${formResult.rawOutput?.slice(0,1400)||'Form unavailable'}\n${injuryResult.rawOutput?.slice(0,1000)||'Injury unavailable'}\n${sentimentResult.rawOutput?.slice(0,900)||'Sentiment unavailable'}\nWrite FINAL_ANSWER using these exact numbers. If odds are unverified, recommend SKIP/MONITOR and do not call it a value bet.`;try{const r=await mistralPool.call(c=>c.chat.complete({model:'mistral-large-latest',messages:[{role:'system',content:prompt},{role:'user',content:`Write the prediction for ${fixture}`}]as any,temperature:.05,maxTokens:4500}));const content=r.choices?.[0]?.message?.content;if(typeof content!=='string'||!content)throw new Error('Empty synthesis response');const finalAnswer=content.includes('FINAL_ANSWER:')?content.slice(content.indexOf('FINAL_ANSWER:')+13).trim():content;await saveCheckpoint({sessionId,agentName:'QuantSynthesis',messages:[],iteration:1,steps:[...steps],rawOutput:finalAnswer,accumulatedData:{trueProb,impliedProb,expectedValue:ev,starRating:scored.starRating,dataCompletenessScore:scored.dataCompletenessScore,isValueBet:scored.isValueBet,recommendedStake:scored.recommendedStake,recommendedOdds:selectionOdds??0},savedAt:Date.now(),version:3});return{finalAnswer,steps,success:true,monteCarlo:{home:mc.homeWin,draw:mc.draw,away:mc.awayWin,stdDev:mc.stdDev},trueProb,impliedProb,expectedValue:ev,starRating:scored.starRating,dataCompletenessScore:scored.dataCompletenessScore,isValueBet:scored.isValueBet,recommendedStake:scored.recommendedStake,recommendedOdds:selectionOdds??0,confidence:Math.round(scored.dataCompletenessScore),goalStatement:`${fixture} — ${market} — True ${(trueProb*100).toFixed(1)}% vs implied ${(impliedProb*100).toFixed(1)}%`,categoryProbabilities:scored.categoryProbabilities}}catch(e){const msg=e instanceof Error?e.message:String(e);return{finalAnswer:'',steps,success:false,error:msg,monteCarlo:{home:mc.homeWin,draw:mc.draw,away:mc.awayWin,stdDev:mc.stdDev},trueProb,impliedProb,expectedValue:ev,starRating:scored.starRating,dataCompletenessScore:scored.dataCompletenessScore,isValueBet:false,recommendedStake:'SKIP',recommendedOdds:selectionOdds??0,confidence:0,goalStatement:`${fixture} — ${market}`,categoryProbabilities:scored.categoryProbabilities}}}}
